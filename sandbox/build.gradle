apply plugin: 'java'
apply plugin: 'application'

mainClassName = "NewClass"

repositories{
    mavenCentral()
}

dependencies {
    testCompile group: 'org.testng', name: 'testng', version: '7.1.0'
}
test {
    // enable TestNG support (default is JUnit)
    useTestNG()
}